# DevNet Day 3 Lab

This project contains implementation of Day 3 REST API lab.

⚠️ Note:
API host `library.demo.local` was not reachable from macOS environment,
so live API artifacts could not be generated.

Project includes:
- Python script
- JSON schema
- pytest validation
- structured artifacts
